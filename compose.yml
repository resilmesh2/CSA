services:
  resilmesh-sap-csa-worker:
    container_name: resilmesh-sap-csa-worker
    build: .
    restart: unless-stopped
    volumes:
      - ./config/config.yaml:/app/config/config.yaml
    networks:
      - resilmesh_network
